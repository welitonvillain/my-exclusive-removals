import { createStitches } from "@stitches/react";

export const {
  config,
  styled,
  css,
  globalCss,
  keyframes,
  getCssText,
  theme,
  createTheme,
} = createStitches({
  theme: {
    colors: {
      yellow: "#FACC14",
      green: "#4ADE80",
      blue: "#60A5FA",
      black: "#131313",
      white: "#FFFFFF",
    },
  },
});
